<!DOCTYPE html>
<html>
  <body>
    <form action="/image" method="post" enctype="multipart/form-data"></div>
      <input type="file" name="image" />
      <input type="submit" name="submit" value="Upload" />
    </form>
    <% @images.each do |upload| %>
      <img src="<%= upload.file.url %>" />
    <% end %>
  </body>
</html>
