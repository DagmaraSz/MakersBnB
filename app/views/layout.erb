<!DOCTYPE html>
<html>
  <head>
    <title>MakersBnB</title>
  </head>

  <body>
    <% if @current_user %>
      Succesful login,  <%= @current_user.email %>
      <form method='post' action='/sessions'>
        <input type='hidden' name='_method' value='delete'>
        <input type='submit' value='Log out'>
      </form>
    <% end %>
    <% if flash[:notice] %>
      <div id='notice'>
        <%= flash[:notice] %>
      </div>
    <% end %>

   <%= if @bookings.space[0].include? (@current_user) then @message = 'No request were made on your spaces'%>
      <%= @message %>
    <%= yield %>
  </body>
</html>
